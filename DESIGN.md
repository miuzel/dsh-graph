# dsh-graph 设计文档

> 基于图的目标管理（Graph-based Goal Management）DeepSeek Harness 插件体系
>
> 设计源流：工业应急 SOP 图模型（见 `reextract-industrial-sop/` 与样例
> `升压变主保护动作跳闸应急处理作业标准.json`）向通用项目管理域的迁移。

## 1. 背景与动机

工业 SOP 项目证明了一套"证据驱动的目标 DAG"模型的有效性：

- **取证（acquisition）**：批量优先并行收集、原子证据项、惰性激活（上游结论成立才派发下游取证）；
- **诊断目标（diagnostic targets）**：中间目标消费原始证据，复合目标经 `depends_on_conclusions` 逐规则部分匹配组合下游结论；
- **显式推理**：`inference_rules` 是可审计的 when → conclusion → actions 规则，禁止隐藏推理；
- **动作因果绑定**：每个动作必须由某个具体结论发射，携带事实前提；
- **保守处理不确定性**：矛盾不静默修复，记录并路由人工确认。

AI coding 工具目前的目标管理是线性 todo list：扁平、无依赖、无条件分支、无证据概念、agent 自我宣布完成。本项目把 SOP 图模型迁移过来，补上这些缺口。

与工业 SOP 的关键差异（有意为之）：

1. **无预定义模板**。任务类型太杂（写 feature、修 bug、画海报、做实验、写周报……），每个目标是自描述的自然语言任务，图内容由 LLM 在目标创建时动态实例化。**结构化的是生命周期与求值语义，非结构化的是内容**。
2. **图是动态的**。存在状态回边（打回重做）与运行期加节点（创建 bugfix 目标）。
3. **执行者多态**。agent、人类、会话外的其他工具都可以是执行者。

## 2. 核心概念模型

```
Backlog ──(排期决策)──> Version ──(范围分解)──> Goal × N
  暂存池               【可选】版本聚合层         目标级闭环实例
                 ↑
                 └── Goal 也可以不经版本独立创建、执行、交付（standalone）
```

三层均为同一图模型的实例，粒度不同：

- **Goal（目标）**：最小工作单元，**自足实体**——四阶段闭环不依赖任何版本。自描述自然语言任务 + 动态生成的取证计划与质量判据。
- **Version（版本）**：**可选的聚合层**。一组目标的集合 + supervisor 决策逻辑，用于批量质量管理（集成测试、发布）。版本级结论（"是否可发布"）等价于复合目标：`depends_on_conclusions: {goal-A: ["已交付"], ...}`。
- **Backlog**：暂存池。既包括"打算排期进版本"的目标，也包括还没决定形态的想法。

**解耦原则（负责人指定）**：目标执行与计划制定两部分不紧耦合。允许不设版本直接创建
目标完成工作，适用于：少数小目标组成的简单任务；人机边定内容边摸索、无法预先制定
明确版本计划的探索型任务。版本相关阶段（集成测试、发布）只对版本内目标生效；
独立目标走完整四阶段闭环后即交付。

### 2.1 目标生命周期（四阶段骨架）

统一的简要模型，不区分目标类型：

```
描述目标 → 收集信息 → 执行任务 → 确认产出
```

展开为引擎强制的状态机：

| 阶段 | 状态 | 语义 |
|---|---|---|
| 描述 | `draft` | 明确目标描述、范围、约束 |
| 描述 | `planning` | LLM 生成：上下文卡片占位（§2.2，只登记需要什么方面的资料）+ 质量判据 + 依赖声明。**判据必须先于执行登记**；生成时受工作规则约束（§2.5）并参考长期记忆与技能（§6、§7） |
| 收集 | `collecting` | 填充上下文卡片：人工贴料、supervisor 补充、或派 **subagent** 收集运行（原始内容在 subagent 内消化，只精炼回流——保持执行阶段上下文干净，§8.2 多会话管理）；人工补充细节为按需激活的提问节点 |
| 收集 | `ready` | 信息齐备，等待执行。依赖其他目标产出时此处被门控（依赖未交付不激活执行，但不阻碍其他取证） |
| 执行 | `in_progress` | 一个或多个 attempt 并行执行（§2.4） |
| 确认 | `review` | **完成声明**触发 review：按预先登记的判据逐条核验产出物；多 attempt 时进入对比评审（§3.4） |
| 确认 | 终态判定 | review 结论路由到处置分支（§4） |
| 交付 | `delivered` | 目标交付，结论作为版本级复合求值的输入；同时触发长期记忆提炼（§6.2） |

任意阶段可进入 `blocked`（记录具体阻塞条件），解除后回到原阶段。

### 2.2 上下文卡片模型（Context Cards）

目标 Runner 的上下文由两部分构成：**一组上下文卡片（种子上下文）** + **runner 运行
时自行工具检索（补充）**。卡片是底座，不是天花板。

- 卡片是**独立实体**（看板上以独立卡片列出，一目了然），初始只登记**标题与形态**
  （需要哪方面的资料：文本/文件/图片/数据）。**不预设查什么、怎么查**——这些在
  实际收集运行时填充；
- 生命周期：`empty`（占位）→ `collecting`（收集中）→ `filled`（内容入库）→
  `reviewed`（可选复核）；
- 填充来源多态：人工直接贴/上传、supervisor 补充、agent 收集运行。重的收集任务
  （需要 agent 运行 + 人工多次干预/回答/review）**在目标之外独立存在**，可升级为
  子目标，其交付物回填卡片；
- attempt 启动时，supervisor 把 `filled/reviewed` 状态的卡片按 `context_cards`
  顺序注入 runner 上下文，**注入清单写入运行履历**（可追溯"runner 当时看到了什么"）。

### 2.3 依赖模型：依赖即数据流

- 边**不需手工声明**。目标 B 在"收集信息"阶段声明"需要目标 A 的产出"，边 A→B 自然诞生。依赖发现并入取证环节，不产生额外用户负担。
- 门控语义复用复合目标的**逐规则部分匹配**：B 只等它实际声明需要的 A 的结论，不等版本内其他目标。
- 引擎在建边时必须做**环检测**，发现循环依赖要报出并路由人工裁决。
- 依赖边携带**产物引用**（A 的哪些交付物/结论被 B 消费），保证可追溯。

### 2.4 执行模型：多态执行者与并行 PK

**执行者多态**：执行节点只关心**产出入库**（产出存在于预期交付位置），不关心谁做的。合法执行者包括：DSH subagent、人类直接在工作区操作、会话外的其他 agent/工具。

**Attempt（尝试）模型**：一次执行 = 一个 attempt，记录执行者、沙盒位置、交付目录、独立履历。一个目标默认单个 attempt，可配置为 **N 路并行 PK**：

- 同一目标（含同一份预先登记的判据）派发给 N 个**相互独立**的执行者——不同模型/prompt 的 agent、人类、或混编；
- 每个 attempt 拥有**独立沙盒环境**（git worktree / 独立目录 / 容器，按项目能力配置）与**独立交付目录**（如 `.dsh-graph/attempts/<goal-id>/<attempt-id>/`），attempt 之间互不可见；
- **判据先于执行是 PK 公平性的保证**：N 份交付物对同一份登记判据独立核验，再进入对比评审（§3.4）；
- PK 为**目标级配置**，默认关闭——成本成倍，只用于高风险或高不确定目标。

**完成声明**：任何执行者（含人工在看板置"已完成"）都只是发起完成声明，触发 review。工作没实际做，review 自然不通过。**状态不是证据，产出物才是。**

### 2.5 整体工作规则约束

不隶属于任何单一目标的**元约束**，对所有目标生效：编码规范、禁止操作、安全策略、预算上限、行文风格等。（对应工业 SOP skill 的 "Non-negotiable constraints" 区。）

- **分层**：全局 → 项目 → 版本，下层可增补不可违背上层；
- **三个生效时机**：planning 时约束 LLM 生成的计划与判据；执行时注入执行者上下文；review 时作为通用否决项（违反规则直接不通过，无需逐条判据）；
- **可机器检查的子集**（如 lint 配置）优先固化为脚本，其余为自然语言约束；
- 规则本身版本化管理；**规则变更写入运行履历**，受影响在途目标的相关判据标记为需复核。

## 3. 质量判据与 Review

### 3.1 判据形式

判据在 planning 阶段由 LLM 生成、可被人工修改，两种形式可混合：

1. **自然语言判据**：由 review 执行者（人或 AI）对照产出物判断；
2. **检查脚本判据**：形如"通过 `scripts/check_xxx.sh` 检查"，适用于复杂或要求确定性的目标。

### 3.2 检查脚本信任模型

- 检查脚本属于**验收方**，不是交付物的一部分；
- 约定：脚本路径必须指向**目标工作范围之外**；目标工作范围内出现该脚本路径的变更时，自动触发重新 review；
- 脚本变更视同判据变更，需经过与判据修改相同的确认路径。

### 3.3 Review 执行者配置

每个 review 决策点可独立配置（项目级默认 + 单目标覆盖）：

- **人工审核**：路由给负责人；
- **AI 审核**：默认提示词；
- **AI 审核 + 自定义提示词**：用户注入特定关注点。

默认策略类似工业 SOP 的 `requires_human_review`：机器结论默认需人工复核，除非用户显式授权自动通过。多人场景下强制 **reviewer ≠ executor**（自己做的事不能自己验收）。

### 3.4 对比评审（PK 模式专用）

多 attempt 目标的 review 分两步：

1. **独立核验**：每个 attempt 的交付物对同一份登记判据逐条核验，得到各自的通过情况；
2. **横向对比**：在通过判据的 attempt 间比较优劣，路由到三种结论之一：
   - **选定胜者**：合并该 attempt 的交付物，目标交付，其余 attempt 归档留存（作为长期记忆素材）；
   - **合并**：取各 attempt 之长人工/AI 合并为最终交付物，合并结果需再过一遍判据；
   - **全灭**：无一通过判据，进入打回分支（可分析失败模式后重开一轮 PK）。

## 4. 处置分支（Review 结论路由）

Review 结论按预登记规则路由，不是执行者临场决定。标准分支（项目可自定义）：

| 结论 | 默认路由 |
|---|---|
| 完全实现 | 关闭目标 → `delivered` |
| 部分实现 | 创建后续目标（继承未竟事项与既有证据），或经决策砍范围交付 |
| 遗留问题 | 创建 bugfix 目标（动态加节点），本目标视规则判定交付或打回 |
| 未实现 | 打回重做（状态回边） |

**状态回边语义**：打回不是清空重来。产出已被修改后，先前"测试通过"等证据实际已失效——复用工业 SOP 的 `freshness` 概念：打回时把受影响证据标记为 `stale`，重新激活对应取证节点，未受影响的证据保留。

## 5. 版本 Supervisor

负责人与 agent 共同驱动，职责：

1. **版本范围管理**：与负责人商定版本内目标与范围（从 backlog 排期入版）；
2. **交付聚合**：收集合并各目标交付情况，求值版本级复合结论；
3. **集成测试决策**：安排集成测试，或显式决策跳过——"跳过"本身必须是一条有理由的显式结论，不是"没做"；
4. **整体质量管理**：提供人工测试入口、测试数据编制支持；
5. **不合格处置**：打回目标开发，或创建 bugfix 目标继续迭代；
6. **模式发现**：识别重复出现的任务模式，提议沉淀为技能（§7）；判定哪些经验升入长期记忆（§6.2）；
7. **发布**：版本终态动作。

**自动化边界由用户决定**：supervisor 的每个决策点独立配置为 人工 / AI 默认 / AI + 自定义提示词。

## 6. 记忆体系

### 6.1 短期记忆（目标内）

载体已存在：目标的**证据台账**（每条证据的来源、值、时间、freshness）与**执行履历**（谁在何时基于什么证据做了什么决策）。随目标归档而封存。

### 6.2 长期记忆（跨目标）

跨目标、跨版本沉淀的知识：

- **成功图**：已完成目标的完整结构（计划、判据、执行路径），作为后续同类目标 planning 的参考输入（few-shot 式）；
- **失败模式**：review 打回原因、PK 全灭分析、误判教训；
- **偏好与约定**：用户在 review 中反复表达的倾向、项目惯例；
- **技能索引**：已沉淀技能与适用场景的映射（§7）。

**提炼（promotion）机制**：目标交付时由 supervisor 判定哪些经验值得升入长期记忆（可配置自动化边界）；记忆条目必须**携带来源目标引用**，可审计、可回溯、可随证据过期而作废。

**检索时机**：后续目标 planning 与 review 时按相关性注入 LLM 上下文——长期记忆不改变生命周期语义，只提高生成质量。

## 7. 技能体系

技能 = 可复用的任务操作知识，采用 DSH 原生 SKILL.md 格式（`reextract-industrial-sop/` 即范例）。两条产生路径：

1. **前瞻式**：supervisor 对已完成目标聚类、发现重复任务模式 → 提议"先建 skill 再做下一次"（决策点，自动化边界可配）；
2. **回溯式**：某目标的 first run 探索成功 → 把成功路径（取证清单、判据模式、坑与对策）沉淀为 skill。

**创建 skill 本身建模为一个 meta-goal**：交付物是 SKILL.md 文件，判据是"下次同类任务能据此完成"，走完整四阶段闭环——技能生产也受"判据先于执行"约束，不允许写出未经检验的技能。

技能被后续目标在 planning 阶段引用；技能与模板的区别：模板是数据结构，技能是可执行的操作规程，二者可互相转化（§10）。

## 8. 插件体系架构

"一切皆为插件"，但插件之下有一个共享核心层（数据模型与求值语义的唯一权威）。
打包形态经源码调研定为**两个 npm 包**（cordis 插件）：

- **`dsh-graph-host`**：核心层 + 目标闭环 + 版本管理 + backlog + 协作层，同一个
  host 插件包（共享核心层必须同包，避免跨包私有状态）。`apply(ctx)` 注入
  `tools` / `subagents` / `agents` / `sessions` / `skills` / `webServer`；
- **`dsh-graph-client`**：看板 client-plugin（`exports["./client"]` + `dsh.client`
  声明，`lib/client.js` 经 `window.__ModuleLoader__` 注册）。

逻辑分层（同包内的模块边界）：

```
┌─────────────────────────────────────────────────────┐
│  kanban 可视化（dsh-graph-client，二维泳道+交互入口）   │
├──────────┬───────────┬──────────┬───────────────────┤
│ 目标闭环  │ 版本管理   │ backlog  │ 协作层（多人场景）   │
│ 模块      │ 模块       │ 模块     │ 角色/权限/通知      │
├──────────┴───────────┴──────────┴───────────────────┤
│  核心层：图数据模型 + 存储 + 求值引擎                   │
│  project/version/goal/attempt JSON 模型、状态机、      │
│  依赖求值、证据台账、运行履历、环检测、                 │
│  规则库、长期记忆库、技能索引                           │
└─────────────────────────────────────────────────────┘
```

### 8.1 核心层（core）

- JSON 数据模型，存于 workspace（如 `.dsh-graph/` 目录），git 友好，与工业 SOP 的 `sop_new/` 同风格；
- 状态机推进、依赖求值（逐规则部分匹配）、证据与 freshness 台账、完整运行履历；
- 规则库（§2.5）、长期记忆库（§6.2）、技能索引（§7）的统一存储与检索；
- 写路径一律经 `writeFileAtomic`，多写者场景叠加 `withFileLock`（`dsh-atomic-write`，
  均为零依赖小函数，v0.1 可直接内联等价实现，成插件后改依赖官方包）；
- 对上层模块暴露操作接口；核心层本身不做任何 LLM 调用决策。

### 8.2 目标闭环（goal-loop 模块）

驱动单目标走完全生命周期：生成取证计划与判据 → 收集信息 → 派发 attempt（含 PK 沙盒编排）→ 按判据核验 → 处置分支路由。

**图操作工具化**：经 `ctx.tools.register(defineTool(...))` 向 agent 暴露图操作
（创建/迁移/登记证据/声明完成/提交 review 结论等），工具用 `presentCall/presentResult`
自持看板友好的 UI 卡片。

**多会话管理**：插件维护收集/执行 subagent 的会话生命周期，**直接映射
`ctx.subagents`**：创建 = `startContinuable`（持久化可续轮），复用收集 agent 转执行 =
`followup`，干预 = `interrupt`，状态查询 = `listChildren/listDescendants`；子级结算
通知由 DSH 管理器自动投递父级。插件只负责 **attempt ↔ childId 绑定**与
创建/复用/归档决策记录（进运行履历），不自建任何会话机制。收集阶段的工具调用与
原始内容在 subagent 内消化，只有精炼证据回流主上下文，保持执行阶段上下文干净、
提高缓存命中率。

### 8.3 版本管理（version-supervisor 模块）

实现 §5 全部职责；打回与 bugfix 走核心层的状态回边与动态加节点接口；模式发现产出技能提案与记忆提炼提案。

### 8.4 Backlog 模块

未排期目标的 CRUD 与排期移动，最薄的一层。

### 8.5 协作层（多人场景，同包后置模块）

核心层保持用户无关；多人语义全部在此层：**角色**（认领者 / 判据所有者 / 审核者）、**权责分离**（reviewer ≠ executor）、认领与并发冲突解决、权限（谁能改判据、谁能置完成、谁能打回）、通知与订阅（依赖上游交付时通知下游）。

### 8.6 Kanban 可视化（dsh-graph-client）

- **二维泳道**：横轴 = 生命周期阶段（描述 / 收集 / 执行 / 确认 / 交付），纵轴 = 版本分组 + 独立目标区 + backlog 区。目标卡片从左游到右；跨泳道依赖边连线渲染；PK 目标在卡内展示 N 路 attempt 进度；
- **观察**：点开卡片查看详情、取证任务、attempt 状况、交付物、判据逐条核对结果、运行履历；
- **干预**：干预个别任务运行；手动执行并声明完成；CRUD 目标与结构；给 supervisor 提供人类输入/建议；调整方向后由 agent 自行调整图；
- **挂点**（调研定稿）：主视图注册进 `conversation.view` 槽（二维泳道整页）；入口按钮进
  `conversation.session.header.actions`；卡片详情用 `details` 槽。会话/工作区数据经
  `useSessions/useWorkspaces`（必须从 `/client` 子路径导入）；
- **数据通道**：host 侧 `ctx.webServer.register`（HTTP）/ `registerUpgrade`（WS）自建
  图数据端点（api-remotes 白名单外），client 侧 `ctx.connection` 消费；
- **HMR**：需 `pnpm run dev:web` watcher 运行才热更，重载丢 React 状态（数据层不丢）。

## 9. 与 DSH 机制的映射（源码调研定稿）

调研基准：`@deepseek-ai/dsh@0.1.0-rc.7` 各包 README 与 lib 类型定义（调研报告见
g-101 证据 ev-01/ev-02）。DSH 是 cordis 插件体系：插件即 `apply(ctx)` 函数，注入
`ctx.<service>`。

| 图模型概念 | DSH 机制（包） | 备注 |
|---|---|---|
| 目标状态机与图存储 | **自建**（`dsh-goal` 经调研确认为会话级单一目标 + 轮次续跑，不可承载三层图） | 核心层唯一必须自建的部分 |
| 图文件原子写 | `writeFileAtomic` / `withFileLock`（`dsh-atomic-write`） | 防半写、跨进程互斥 |
| 图操作暴露给 agent | `ctx.tools.register(defineTool(...))`（`dsh-tools`） | 工具可自持 UI 卡片（presentCall/presentResult） |
| 取证/执行 subagent 多会话 | `ctx.subagents`：`startContinuable` / `followup` / `interrupt` / `listChildren`（`dsh-subagent`） | 会话持久化 + 冷恢复内置；子级结算自动通知父级；复用收集 agent 续轮进执行 = 一次 `followup` |
| attempt 履历 | `dsh-session-persistence-jsonl`（root 指向 `.dsh-graph/` 下目录） | 子 agent 会话即 attempt 的完整 transcript |
| PK 沙盒隔离 | git worktree / 独立交付目录 | 无对应 DSH 机制，自建 |
| 人工补充细节、review 人工节点 | `ask_user_question`（`dsh-tool-ask-user`） | |
| 技能沉淀 | 写 SKILL.md（`dsh-skill-filesystem` 扫描 `.dsh/skills` 等目录）或 `ctx.skills.register()` 运行时注册（`dsh-skill`） | 两条路都保留 |
| 看板界面 | client-plugin：`ctx.slots.inject/register`（`dsh-client-runtime`） | 挂点见 §8.6 |
| host↔client 自定义通道 | `ctx.webServer.register/registerUpgrade`（`dsh-host-webserver`） | 见下方限制 2 |
| 阶段进度投影 | `todo_write`（仅展示视图，非真相源） | |

**调研确认的限制与绕过**：

1. `dsh-goal` 不可复用为图状态机（会话级、单目标、事件溯源）——核心层自建；
2. client↔host 自定义 RPC/事件**不在** `dsh-api-remotes` 构建期白名单内——看板数据通道走
   `ctx.webServer` 自注册 HTTP/WS 端点，client 侧经 `ctx.connection` 消费；
3. 可继续子 agent 的 `followup` 要求**确切在线直接父级**，驻留仅进程内——宿主重启后靠
   持久化会话冷恢复续轮；崩溃可能丢"已接受未写日志"的消息；
4. 一次性 `SubagentRun` 无续轮——凡需复用的收集/执行 agent 一律走 `startContinuable`；
5. 工具的 `timeoutMs` 仅声明不强制，需要时限时叠加 `dsh-tool-call-timeout-policy`；
6. client HMR 仅在 `pnpm run dev:web` watcher 运行时生效，且重载丢 React 状态；
7. **人工 gate 流程不能用 goal round 自动续轮驱动**（dogfood 发现#3）——人工决策点必须
   停轮等输入。

## 10. 演进路径：从自由图到沉淀

1. **现阶段**：无模板，图由 LLM 按目标动态生成；
2. **使用后沉淀**：成功图进入长期记忆，作为同类目标 planning 的参考输入；
3. **技能化**：重复模式被 supervisor 发现，沉淀为 DSH skill（前瞻或回溯）；
4. **模板化（远期）**：从成功图归纳稳定数据结构为可选模板——模板是**加速选项而非强制约束**，与"无模板"原则不冲突。

## 11. 适用范围

本模型适用于**有可检查交付物的项目型工作**——coding、写作、设计、研究、实验、筹办等，不局限于 coding。跨域时滑动的是判据的确定性光谱（脚本可判定 ↔ 只能人判），模型以 NL 判据 + 脚本判据混合兼容全光谱；交付物是"世界状态"（如达成共识）时需引入证据代理物（会议纪要、确认邮件）。

**不适用**：无可检查交付物的工作（习惯养成、关系维护）；无法预设判据的纯开放探索（缓解：重定义为知识交付目标）；高频琐碎任务（缓解：轻量路径——免规划、自判据、抽查式 review，严格程度按目标风险分级配置）。

## 12. 关键设计原则（不可妥协）

1. **判据先于执行**：质量标准在 planning 阶段登记，review 是规则求值而非主观印象；这也是 PK 公平性的前提；
2. **状态不是证据，产出物才是**：任何"完成"都是声明，必须经判据核验；
3. **显式推理**：所有路由决策来自登记的规则，可审计、可回放；
4. **执行者多态**：引擎只认证据，不认执行者；多人场景 reviewer ≠ executor；
5. **不静默修复**：矛盾与缺陷记录并路由人工，宁可 blocked 不可猜测；
6. **惰性激活**：下游工作只有在上游结论成立后才派发；
7. **自动化边界用户说了算**：每个审核/决策点独立可配；
8. **经验必须沉淀**：成功图、失败模式、技能进入长期记忆，系统越用越聪明；
9. **目标自足，版本可选**：目标闭环不依赖版本模块；独立创建的目标与版本内目标
   走同一生命周期，版本只追加聚合与批量质量门。
