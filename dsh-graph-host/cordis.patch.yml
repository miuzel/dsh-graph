# dsh-graph-host 的 bundle 层：以裸包名插入 profile 树（dsh plugin add 时自动激活）
# root 相对 workspace 根（process.cwd()），与 client 同一解析 resolve(workspaceRoot, config.root ?? ".dsh-graph")（g-112）。
- insert:
    - id: dsh-graph-host
      name: dsh-graph-host
      config:
        root: .dsh-graph
