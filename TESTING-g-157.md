# g-157 拖动自动滚动功能测试说明

## 测试环境
- 测试实例：http://127.0.0.1:3082
- 主实例：http://127.0.0.1:3080（已同步代码）

## 测试步骤

### 1. 准备测试数据
1. 访问测试实例（3082）
2. 创建一个有多个版本泳道的看板
3. 确保看板内容足够长，需要滚动才能看到所有内容

### 2. 测试自动滚动功能
1. 拖动一个 goal 卡片
2. 将指针移动到浏览器视口顶部附近（距离顶部 80px 以内）
3. 验证页面是否自动向上滚动
4. 将指针移动到浏览器视口底部附近（距离底部 80px 以内）
5. 验证页面是否自动向下滚动

### 3. 测试停止功能
1. 拖动卡片时，将指针移动到触发区域外
2. 验证自动滚动是否停止
3. 松开鼠标（结束拖动）
4. 验证自动滚动是否完全停止

### 4. 测试边界情况
1. 在页面顶部时，尝试向上滚动（不应滚动）
2. 在页面底部时，尝试向下滚动（不应滚动）
3. 快速移动指针进出触发区域

## 预期行为

1. **激活条件**：仅在拖动 goal 卡片时自动滚动
2. **触发区域**：指针距离视口顶部或底部 80px 以内
3. **滚动速度**：最大 20px/帧，根据距离平滑调整
4. **停止条件**：
   - 拖动结束（松开鼠标）
   - 指针离开触发区域
   - 页面已滚动到顶部/底部
5. **不影响其他操作**：普通点击、悬停或未拖动时不应自动滚动

## 已知限制

1. 仅处理垂直页面滚动，不处理横向滚动
2. 不滚动独立内部容器，仅滚动整个页面
3. 在测试实例中验证，主实例已同步代码

## 代码位置

- 源文件：`dsh-graph-host/lib/client/kanban.js`
- 生成文件：`dsh-graph-host/lib/client.js`
- 提交：`feat(g-157): 实现拖动自动滚动功能`
